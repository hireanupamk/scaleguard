# Jira integration environment example
# Copy this file to .env if you want to centralize non-secret configs.
# Note: OAuth clientId/clientSecret are stored in scripts/jira/jira.oauth.json (gitignored). Do NOT commit secrets.

# Base URL for your Jira Cloud site
JIRA_BASE_URL=https://scaleguard.atlassian.net

# Default project key used by scripts (can be overridden via --project)
JIRA_PROJECT_KEY_DEFAULT=PS

# Redirect URL configured in your Atlassian OAuth app
JIRA_REDIRECT_URI=http://localhost:43123/callback

# The following are intentionally omitted here because they are secrets:
# JIRA_CLIENT_ID=
# JIRA_CLIENT_SECRET=

# After creating your OAuth app:
# 1) Copy scripts/jira/jira.oauth.template.json to scripts/jira/jira.oauth.json
# 2) Paste CLIENT_ID and CLIENT_SECRET there
# 3) Run: npm run jira:auth
